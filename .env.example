# ===========================================
# LIST SCRUBBER AI - Environment Configuration
# ===========================================
# Copy this file to .env.local and fill in your values
# NEVER commit .env.local to version control

# ===========================================
# SUPABASE CONFIGURATION
# ===========================================
# Find these in: Supabase Dashboard > Project Settings > API
#
# Project URL - Your unique Supabase project URL
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Publishable Key (anon/public) - Safe to expose in browser
# Used for client-side operations with Row Level Security
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Secret Key (service_role) - NEVER expose in browser
# Used for server-side admin operations that bypass RLS
SUPABASE_SECRET_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ===========================================
# STRIPE CONFIGURATION
# ===========================================
# Find these in: Stripe Dashboard > Developers > API keys

# Restricted Key - NEVER expose in browser
# Create at: Stripe Dashboard > Developers > API keys > Create restricted key
# Required permissions:
#   - Checkout Sessions: Write
#   - Customers: Write
#   - Customer portal: Write
#   - Subscriptions: Write
#   - Invoices: Read
#   - Payment Methods: Read
#   - Webhook Endpoints: Read
STRIPE_RESTRICTED_KEY=rk_test_your_restricted_key

# Publishable Key - Safe to expose in browser
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key

# -------------------------------------------
# STRIPE WEBHOOK CONFIGURATION
# -------------------------------------------
# Webhooks allow Stripe to notify your app about events (payments, subscriptions, etc.)
#
# HOW TO SET UP WEBHOOKS:
#
# FOR LOCAL DEVELOPMENT:
# 1. Install the Stripe CLI: https://stripe.com/docs/stripe-cli
# 2. Login to Stripe CLI:
#    $ stripe login
# 3. Forward webhooks to your local server:
#    $ stripe listen --forward-to localhost:3000/api/webhooks/stripe
# 4. The CLI will display a webhook signing secret (starts with whsec_)
# 5. Copy that secret and paste it below
#
# FOR PRODUCTION (Vercel, etc.):
# 1. Go to: Stripe Dashboard > Developers > Webhooks
# 2. Click "Add endpoint"
# 3. Enter your endpoint URL: https://your-domain.com/api/webhooks/stripe
# 4. Select events to listen to:
#    - checkout.session.completed
#    - customer.subscription.created
#    - customer.subscription.updated
#    - customer.subscription.deleted
#    - invoice.payment_succeeded
#    - invoice.payment_failed
# 5. Click "Add endpoint"
# 6. Click "Reveal" under Signing secret and copy the whsec_... value
#
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# -------------------------------------------
# STRIPE PRODUCT/PRICE CONFIGURATION
# -------------------------------------------
# Create products in: Stripe Dashboard > Products
#
# HOW TO CREATE PRODUCTS:
# 1. Go to: Stripe Dashboard > Products > Add product
#
# 2. Create STANDARD plan:
#    - Name: "Standard"
#    - Price: $0.00 (or leave as free)
#    - Billing period: Monthly
#    - Click "Save product"
#    - Copy the Price ID (starts with price_)
#
# 3. Create PRO plan:
#    - Name: "Pro"
#    - Price: $29.00
#    - Billing period: Monthly
#    - Click "Save product"
#    - Copy the Price ID (starts with price_)
#
STRIPE_STANDARD_PRICE_ID=price_standard_monthly
STRIPE_PRO_PRICE_ID=price_pro_monthly

# ===========================================
# OPENROUTER CONFIGURATION
# ===========================================
# Find this at: openrouter.ai > Keys
OPENROUTER_API_KEY=sk-or-your_openrouter_api_key

# ===========================================
# VERCEL CONFIGURATION (for deployments)
# ===========================================
VERCEL_URL=your-app.vercel.app

# ===========================================
# APPLICATION CONFIGURATION
# ===========================================
NEXT_PUBLIC_APP_URL=http://localhost:3000
